<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">PicStory</a>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
      <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-user-circle-o" style="font-size:24px""></i>
            <span class="caret"></span>
          </a>
          <!-- ¡Pending implementation! Should work toggling between sign_in and sign_out -->
          <ul class="dropdown-menu">
            <li><%=link_to 'Create PicStory', new_story_path %></li>
            <li><%=link_to 'My PicStories', stories_path %></li>
            <% if session[:user_id].present? %>
              <li><a href="<%= user_path(session[:user_id]) %>">'My Profile'</a></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <% if session[:user_id].present? %>
              <li><%= link_to 'Sign Out', destroy_session_path, method: :post%></li>
            <% else %>
              <li><a href="<%= sessions_new_path %>">'Sign In'</a></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
